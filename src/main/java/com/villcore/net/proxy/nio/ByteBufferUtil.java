package com.villcore.net.proxy.nio;

import java.nio.ByteBuffer;

/**
 * Created by Administrator on 2017/7/15.
 */
public class ByteBufferUtil {
    public static String getContent(ByteBuffer byteBuffer) {
//        byte[] bytes = byteBuffer.array();
//        byteBuffer.rewind();
//        byteBuffer.position(4 + 4 + 8 + 4);
//        String content = new String(bytes, byteBuffer.position(), byteBuffer.limit() - byteBuffer.position());
//        byteBuffer.rewind();
//        return content;
        return "";
    }
}
